import { css } from "lit";

export const localStorageStyles = css`
    .container {
        display: block;
        position: absolute;
        right: 8px;
        top: 16px;

        input[type="text"] {
            width: 200px;
            padding: 4px;
            border: 1px solid #ccc;
        }
    }
`;
